# Groq API key for the AI Predictive Lab (/api/predict route).
# Copy this file to `.env` and paste your key from https://console.groq.com/keys
# For Vercel: add GROQ_API_KEY under Settings → Environment Variables in the
# project dashboard; the /api/predict serverless function reads it at runtime.
GROQ_API_KEY=